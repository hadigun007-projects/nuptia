# ==============================================================================
# Nuptia - Production Environment Configuration
# ==============================================================================

# Node Environment
NODE_ENV=production

# Host Ports
WEB_PORT=3000
HTTP_PORT=80
HTTPS_PORT=443

# Domain Name (for Nginx and SSL)
DOMAIN=nuptia.id

# Backend API Port (Prepared for future api module)
API_PORT=5000
