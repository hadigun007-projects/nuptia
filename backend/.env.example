# ==============================================================================
# Nuptia Backend Environment Configuration
# ==============================================================================

# Server Environment
PORT=5000
GIN_MODE=debug

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=hadiyahku
DB_PASSWORD=
DB_NAME=nuptia_db
DB_SSLMODE=disable

# CORS Configuration (comma-separated allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175

# Authentication Configuration
JWT_SECRET=nuptia-super-secret-jwt-key-2026-wedding-platform
GOOGLE_CLIENT_ID=

# Email — Development (Mailpit: https://mailpit.axllent.org)
# Jalankan: mailpit | SMTP → localhost:1025 | Web UI → localhost:8025
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
APP_BASE_URL=http://localhost:5174
AUTH_APP_URL=http://localhost:5175
